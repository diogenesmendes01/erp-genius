import { describe, expect, it, vi } from "vitest";
import { renderToStaticMarkup } from "react-dom/server";
const { preparar, historico } = vi.hoisted(() => ({ preparar: vi.fn(), historico: vi.fn() }));
vi.mock("@/server/_shared", () => ({ exigirSessaoPagina: vi.fn() }));
vi.mock("@/server/avaliacoes/conferencia-regra-historica", () => ({ consultarPreparacaoConferenciaRegraHistorica: preparar, consultarConferenciasRegraHistorica: historico }));
vi.mock("./ConferenciaRegraHistorica", () => ({ PrepararConferenciaRegraHistorica: () => "Seleção histórica", DecidirConferenciaRegraHistorica: () => "Decisão histórica" }));
vi.mock("../../../[nivelId]/ResumoRegra", () => ({ ResumoRegra: () => "Conteúdo congelado" }));
import Page from "./page";
const turma={id:"t",nome:"Turma legada",codigo:null,nivelId:"n",regraAvaliacaoId:null,nivel:{codigo:"A1",idioma:{nome:"Idioma"}}};
describe("página da conferência histórica",()=>{it("oferece seleção sem regra e conserva fotografia/histórico após a vinculação",async()=>{preparar.mockResolvedValueOnce({ok:true,dado:{turma,destinos:[{id:"r1",versao:1,conteudo:{},conteudoHash:"a"}],pendencia:null}}).mockResolvedValueOnce({ok:true,dado:{turma:{...turma,regraAvaliacaoId:"r1"},destinos:[],pendencia:"A turma já possui regra vinculada."}});historico.mockResolvedValue({ok:true,dado:{pagina:1,temProxima:false,itens:[{id:"p",versao:1,estadoHash:"a".repeat(64),motivo:"Motivo válido",evidencia:"Ata válida",criadaEm:new Date("2025-01-01"),preparador:{id:"u",nome:"Preparador"},decisao:{aprovada:true,motivo:"Decisão válida",criadaEm:new Date("2025-01-02"),decisor:{nome:"Decisor"}},podeDecidir:false,podeAprovar:false,snapshot:{turma:{nome:"Turma legada",codigo:null,status:"EM_ANDAMENTO",dataInicio:null,dataFim:null},destino:{versao:1,conteudo:{},conteudoHash:"a"},encontros:[],diarios:[],alocacoes:[]}}]}});const primeiro=renderToStaticMarkup(await Page({params:Promise.resolve({turmaId:"t"}),searchParams:Promise.resolve({})}));expect(primeiro).toContain("Seleção histórica");const depois=renderToStaticMarkup(await Page({params:Promise.resolve({turmaId:"t"}),searchParams:Promise.resolve({})}));expect(depois).toContain("Histórico de conferências");expect(depois).toContain("Conteúdo congelado");});});
